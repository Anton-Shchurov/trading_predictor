# Описание признаков

# 📚 MVP Feature Set (33)

## Правила наименования (naming)

- Все признаки начинаются с `f_` (кроме `close`), слова через подчёркивание, числа — длины окон в барах.
- Тайм-серия обозначается суффиксом `_t` (текущий бар), `_t-k` (k баров назад).

## Базовые обозначения (notation)

- **EMA (exponential moving average)** — экспоненциальная скользящая средняя: `EMA_t = α·close_t + (1-α)·EMA_{t-1}`, где `α = 2/(N+1)`.
- **ATR (average true range)** — средний истинный диапазон Уайлдера (Wilder):
    
    `TR_t = max(High_t-Low_t, |High_t-Close_{t-1}|, |Low_t-Close_{t-1}|)`;
    
    `ATR_t = EMA(TR_t, N)` (у Уайлдера — сглаживание RMA, в практике часто EMA).
    
- **RSI (relative strength index)** — индекс относительной силы Уайлдера: `RSI = 100 - 100/(1+RS)`, `RS = avg_gain/avg_loss`.
- **ROC (rate of change)** — темп изменения: `(close_t - close_{t-N})/close_{t-N}`.
- **VWAP (volume-weighted average price)** — объёмно-взвешенная цена.
- **BB (Bollinger Bands)** — полосы Боллинджера: `BB_mid = SMA(close, N)`, `BB_upper/lower = BB_mid ± k·σ_N`.
- **Keltner Channels** — каналы Кельтнера: `mid = EMA(close, N)`, `bands = mid ± m·ATR_N`.
- **Donchian Channel** — канал Дончиана: минимум/максимум за окно `N`.
- **Z-score** (z-оценка): `(x - mean_N)/std_N`.
- **DMI/ADX (directional movement index / average directional index)** — индикаторы направленного движения и силы тренда (Welles Wilder).
- **PSAR (parabolic stop-and-reverse)** — параболический SAR; бинаризуем тренд по положению цены относительно PSAR.

> Пример нормализации на ATR (ATR-normalization): отклонение в 10 пунктов при ATR=5 = 2 ATR (сильно), при ATR=20 = 0.5 ATR (слабо). Это делает признаки сопоставимыми при разной волатильности (volatility).
> 

---

## Базовый уровень

### 1) `close` — цена закрытия (close)

- **Что показывает:** последнюю цену часа (базовый уровень).
- **Интерпретация:** абсолютная референс-линия для индикаторов.
- **Комментарий:** сама по себе малоинформативна; лучше нормализовать или брать производные.
- **Расчёт:** `close_t`.

---

## EMA (трендовые средние, trend averages)

### 2) `f_ema_10` — EMA10

- **Что показывает:** локальную равновесную цену за ~10 часов с бóльшим весом последних баров.
- **Интерпретация:** `close > EMA10` → локально бычий импульс; `close < EMA10` → медвежий; наклон отражает краткосрочный тренд.
- **Комментарий:** «нервная» (noisy) средняя; лучше смотреть спреды и наклоны (EMA10−EMA20, slope).
- **Расчёт:** `EMA(close, 10)`.

### 3) `f_ema_20` — EMA20

- **Что показывает:** сглаженный уровень ~20 часов.
- **Интерпретация:** цена выше → бычий режим, ниже → медвежий; кроссы EMA10/EMA20 = смена импульса.
- **Комментарий:** надёжнее EMA10, но запаздывает; полезны нормированные расстояния.
- **Расчёт:** `EMA(close, 20)`.

### 4) `f_ema_50` — EMA50

- **Что показывает:** тренд ~2 суток.
- **Интерпретация:** выше → устойчивый бычий режим; ниже → медвежий; фильтрует ложные сигналы коротких EMA.
- **Комментарий:** меньше шума, больше лаг.
- **Расчёт:** `EMA(close, 50)`.

### 5) `f_ema_200` — EMA200

- **Что показывает:** долгосрочный тренд (~8 суток на H1).
- **Интерпретация:** выше → глобально бычий рынок; ниже → медвежий; «золотой крест»/«крест смерти» (golden/death cross).
- **Комментарий:** важен как контекст; на горизонте 5 баров предсказательная сила ограничена.
- **Расчёт:** `EMA(close, 200)`.

---

## Тренд (trend)

### 6) `f_close_minus_ema_20_over_atr14` — расстояние до EMA20 в ATR

- **Что показывает:** отклонение цены от EMA20 с учётом волатильности.
- **Интерпретация:** `>+1`/`<-1` → перекупленность/перепроданность относительно «равновесия».
- **Комментарий:** ATR-нормировка делает признак стабильнее во времени.
- **Расчёт:** `(close_t - EMA20_t) / ATR14_t`.

### 7) `f_ema20_slope` — наклон EMA20 (slope)

- **Что показывает:** изменение EMA20 за бар — направление и скорость тренда.
- **Интерпретация:** `>0` рост, `<0` падение, `≈0` боковик; смена знака → возможный разворот.
- **Комментарий:** при низкой волатильности мала амплитуда; можно делить на ATR.
- **Расчёт:** `EMA20_t - EMA20_{t-1}` *(вариант: `(EMA20_t - EMA20_{t-1})/ATR14_t`)*.

### 8) `f_macd_12_26_9_hist` — гистограмма MACD (momentum of trend)

- **Что показывает:** разницу между MACD и сигнальной линией — ускорение/замедление.
- **Интерпретация:** выше нуля и растёт → усиливающийся бычий импульс; ниже нуля и снижается → медвежий.
- **Комментарий:** в боковике шумит; для H=5 баров может запаздывать, но полезен как индикатор силы.
- **Расчёт:**
    
    `MACD_t = EMA(close,12)_t - EMA(close,26)_t`
    
    `Signal_t = EMA(MACD,9)_t`
    
    `Hist_t = MACD_t - Signal_t`.
    

### 9) `f_di_diff_14` — разница DI⁺−DI⁻ (directional movement)

- **Что показывает:** баланс сил покупателей/продавцов за 14 баров.
- **Интерпретация:** `>0` перевес быков; `<0` медведей; `≈0` равновесие/боковик.
- **Комментарий:** чувствителен к шипам; лучше вместе с ADX (filter).
- **Расчёт:** `DI+_14 - DI-_14` (из DMI Уайлдера).

### 10) `f_adx_14` — сила тренда ADX

- **Что показывает:** силу тренда без направления.
- **Интерпретация:** `0–20` флет, `20–40` тренд, `>40` сильный тренд; рост/падение = усиление/затухание.
- **Комментарий:** запаздывает, но отличный фильтр.
- **Расчёт:** стандартный `ADX(14)` по Уайлдеру.

---

## Импульс (momentum)

### 11) `f_rsi_5` — RSI(5)

- **Что показывает:** ультра-краткосрочный импульс (несколько свечей).
- **Интерпретация:** `>70` перекупленность; `<30` перепроданность; `≈50` баланс.
- **Комментарий:** очень чувствителен; полезен для быстрых импульсов.
- **Расчёт:** `RSI(close, 5)` (по Уайлдеру).

### 12) `f_rsi_14` — RSI(14)

- **Что показывает:** более сглаженный импульс.
- **Интерпретация:** дольше держится в крайних зонах.
- **Комментарий:** надёжнее для фильтрации, но медленнее.
- **Расчёт:** `RSI(close, 14)`.

### 13) `f_roc_5` — ROC(5) (rate of change)

- **Что показывает:** темп изменения цены за 5 баров.
- **Интерпретация:** положительное → ускорение вверх; отрицательное → вниз; по модулю — сила импульса.
- **Комментарий:** простой и быстрый индикатор краткосрочного импульса; может дублировать часть информации `f_ret_6`.
- **Расчёт:** `ROC_5 = (close_t - close_{t-5}) / close_{t-5}`.

---

## Волатильность и диапазон (volatility & range)

### 14) `f_atr_14_pct` — ATR к цене (relative ATR)

- **Что показывает:** относительную волатильность (масштаб рынка) в долях цены.
- **Интерпретация:** выше → рынок «шире ходит»; ниже → «узкий» рынок.
- **Комментарий:** полезно как нормализатор и как самостоятельный контекст.
- **Расчёт:** `ATR14_t / close_t`.

### 15) `f_return_std_20` — std доходностей (volatility of returns)

- **Что показывает:** нестабильность однобаровых лог-доходностей на окне 20.
- **Интерпретация:** выше → рынок дёрганный; ниже → спокойный.
- **Комментарий:** перекрывается с ATR, но измеряет именно вариабельность **доходностей**, а не диапазонов.
- **Расчёт:**
    
    `r1_t = ln(close_t / close_{t-1})`
    
    `f_return_std_20 = std(r1, окно=20)`.
    

### 16) `f_range_over_atr` — диапазон свечи к ATR

- **Что показывает:** размер текущей свечи относительно «нормальной» волатильности.
- **Интерпретация:** `≈1` типично; `>1` импульс/новости; `<1` «сонная» свеча.
- **Комментарий:** помогает ловить аномалии.
- **Расчёт:** `(High_t - Low_t) / ATR14_t`.

### 17) `f_donchian_pos_20` — позиция в канале Дончиана (Donchian)

- **Что показывает:** положение цены в диапазоне [Min, Max] за 20 баров.
- **Интерпретация:** `0` у низа, `1` у верха, `0.5` середина.
- **Комментарий:** хорошо показывает «где мы в диапазоне», но в боковике даёт ложные сигналы пробоев.
- **Расчёт:**
    
    `Low20_t = min(Low_{t-19..t})`
    
    `High20_t = max(High_{t-19..t})`
    
    `f = (close_t - Low20_t) / (High20_t - Low20_t)`.
    

### 18) `f_keltner_pos_20` — позиция в каналах Кельтнера (Keltner)

- **Что показывает:** отклонение цены от EMA20, нормированное ATR20.
- **Интерпретация:** `>+1` пробой вверх, `<-1` вниз; `[-1, +1]` внутри канала.
- **Комментарий:** адаптивен; чувствителен к параметрам.
- **Расчёт:**
    
    `mid_t = EMA(close,20)_t`
    
    `band_t = 2*ATR20_t`
    
    `f = (close_t - mid_t) / band_t`.
    

### 19) `f_percentB_20` — %B Боллинджера (Bollinger %B)

- **Что показывает:** положение внутри полос Боллинджера.
- **Интерпретация:** `0` у нижней, `1` у верхней; `<0`/`>1` — выход за пределы.
- **Комментарий:** хорошо ловит перегрев; в тренде цена «липнет» к границе.
- **Расчёт:**
    
    `BB_mid = SMA(close,20)`
    
    `σ20 = std(close,20)`
    
    `BB_upper = BB_mid + 2·σ20`
    
    `BB_lower = BB_mid - 2·σ20`
    
    `%B = (close_t - BB_lower) / (BB_upper - BB_lower)`.
    

### 20) `f_bandwidth_20` — ширина полос Боллинджера (bandwidth)

- **Что показывает:** относительную ширину BB (volatility proxy).
- **Интерпретация:** низкая → «сжатие», возможен импульс; высокая → уже волатильно.
- **Комментарий:** сила, но не направление.
- **Расчёт:** `(BB_upper - BB_lower) / BB_mid`.

---

## Доходности и статистика (returns & stats)

### 21) `f_ret_1` — лог-доходность за 1 бар

- **Что показывает:** мгновенное изменение цены.
- **Интерпретация:** знак и модуль = направление и сила импульса.
- **Комментарий:** шумный базовый кирпич.
- **Расчёт:** `ln(close_t / close_{t-1})`.

### 22) `f_ret_6` — лог-доходность за 6 баров

- **Что показывает:** изменение за 6 часов.
- **Интерпретация:** устойчивость краткосрочного тренда.
- **Комментарий:** сглаживает шум `f_ret_1`.
- **Расчёт:** `ln(close_t / close_{t-6})`.

### 23) `f_ret_12` — лог-доходность за 12 баров

- **Что показывает:** изменение за полсуток.
- **Интерпретация:** среднесрочный внутридневной уклон.
- **Комментарий:** более устойчива, но ближе к трендовым индикаторам.
- **Расчёт:** `ln(close_t / close_{t-12})`.

### 24) `f_ret_24` — лог-доходность за 24 бара

- **Что показывает:** дневной контекст.
- **Интерпретация:** `>0` выше суток назад; `<0` ниже.
- **Комментарий:** может быть «тяжёлой» для горизонта 5 баров.
- **Расчёт:** `ln(close_t / close_{t-24})`.

### 25) `f_ret_1_z20` — Z-оценка доходности за 1 бар

- **Что показывает:** насколько текущая `f_ret_1` необычна относительно последних 20.
- **Интерпретация:** `|z|>2` — экстремум.
- **Комментарий:** ловит всплески; чувствителен к «плоским» периодам.
- **Расчёт:** `(f_ret_1 - mean(f_ret_1,20)) / std(f_ret_1,20)`.

### 26) `f_rolling_mean_ret_5` — средняя доходность (5)

- **Что показывает:** усреднённый уклон за 5 часов.
- **Интерпретация:** знак = направление локального уклона.
- **Комментарий:** коррелирует с другими доходностями.
- **Расчёт:** `mean(f_ret_1, окно=5)`.

### 27) `f_rolling_std_ret_5` — std доходностей (5)

- **Что показывает:** локальную «турбулентность» доходности.
- **Интерпретация:** выше = резкие движения, ниже = спокойствие.
- **Комментарий:** частично дублирует ATR, но в пространстве r(t).
- **Расчёт:** `std(f_ret_1, окно=5)`.

### 28) `f_zscore_close_50` — Z-оценка цены (50)

- **Что показывает:** отклонение `close` от своей 50-барной нормы.
- **Интерпретация:** `>+2` перекупленность; `<-2` перепроданность; `[-1..+1]` норма.
- **Комментарий:** в тренде может «залипать» в крайностях.
- **Расчёт:** `(close_t - SMA(close,50)_t) / std(close,50)`.

---

## Прочие (other)

### 29) `f_vwap_dev_over_atr14` — отклонение от VWAP в ATR

- **Что показывает:** насколько цена выше/ниже справедливой VWAP.
- **Интерпретация:** `>+1` — перекупленность; `<-1` — перепроданность.
- **Комментарий:** на Forex VWAP по тиковым объёмам → сигнал слабее, чем на акциях.
- **Расчёт:** `(close_t - VWAP_t) / ATR14_t`.

### 30) `f_psar_trend` — тренд по PSAR (binary trend)

- **Что показывает:** бинарный режим тренда.
- **Интерпретация:** 1 = бычий, 0 = медвежий; переключение = потенциальный разворот.
- **Комментарий:** в боковике частые перещёлкивания.
- **Расчёт:** `1`, если `close_t > PSAR_t`, иначе `0`.

---

## Календарные (calendar/time)

### 31–32) `f_hour_of_day_sin`, `f_hour_of_day_cos` — суточная сезонность

- **Что показывают:** фазу торгового дня без разрыва 23→0 (цикличность).
- **Интерпретация:** помогают моделям ловить паттерны сессий (Лондон/Нью-Йорк).
- **Комментарий:** работают в паре (sin+cos).
- **Расчёт:**
    
    `sin = sin(2π·hour/24)`
    
    `cos = cos(2π·hour/24)`.
    

### 33) `f_dow` — день недели (day of week)

- **Что показывает:** 0=Пн … 6=Вс.
- **Интерпретация:** у дней разные поведенческие профили (понедельник вялый, середина активная, пятница фиксация).
- **Комментарий:** как числовой — может вносить ложный порядок; предпочтительно one-hot.
- **Расчёт:** `0…6`.

---

## Режимы (regimes)

### 34) `f_regime_trend_adx14` — флаг тренда по ADX

- **Что показывает:** бинарный индикатор «тренд / нет тренда» на основе ADX14.
- **Интерпретация:** 1, если `ADX14 > 20–25` → рынок в тренде; 0 → боковик.
- **Комментарий:** простой фильтр режимов; помогает разделять ситуации, когда momentum-фичи значимы, и когда они шумят.
- **Расчёт:** `f_adx_14_t > τ` *(τ = 20–25)*.

---

### 35) `f_regime_bull_ema200` — флаг бычьего контекста

- **Что показывает:** находится ли цена выше долгосрочной EMA200.
- **Интерпретация:** 1, если `close > EMA200` → бычий фон; 0 → медвежий.
- **Комментарий:** добавляет контекст глобального тренда; полезно как условие для активации быстрых сигналов.
- **Расчёт:** `close_t > EMA200_t`.

---

### 36) `f_regime_highvol_atr14` — флаг высокой волатильности

- **Что показывает:** превышает ли относительный ATR14 порог (например, 80-й перцентиль).
- **Интерпретация:** 1 → рынок в фазе высокой волы; 0 → нормальный/спокойный режим.
- **Комментарий:** позволяет моделям иначе реагировать на всплески турбулентности.
- **Расчёт:** `f_atr_14_pct_t > Q80`.

---

## Сжатие волатильности (volatility squeeze)

### 37) `f_bb_squeeze_20` — относительное сжатие Боллинджера

- **Что показывает:** степень «сжатия» полос Боллинджера относительно ATR.
- **Интерпретация:** низкие значения → рынок зажат, часто предвестник импульса; высокие → расширение.
- **Комментарий:** индикатор фаз накопления/разрядки.
- **Расчёт:** `f_bandwidth_20 / f_atr_14_pct`.

---

### 38) `f_stdret20_over_atr14` — волатильность доходностей к ATR

- **Что показывает:** стандартное отклонение доходностей на окне 20 баров, нормированное на ATR14.
- **Интерпретация:** >1 → вола доходностей выше типичной ценовой волы; <1 → движение спокойное.
- **Комментарий:** даёт альтернативную меру squeeze/expansion.
- **Расчёт:** `f_return_std_20 / f_atr_14_pct`.

---

## Форма распределения (shape stats)

### 39) `f_rolling_skew_ret_20` — асимметрия доходностей

- **Что показывает:** скошенность (skewness) распределения доходностей r1 на окне 20 баров.
- **Интерпретация:** >0 → преобладают большие положительные бары; <0 → большие отрицательные.
- **Комментарий:** помогает выявлять смещения в поведении рынка.
- **Расчёт:** `skew(r1_{t-19:t})`.

---

### 40) `f_rolling_kurt_ret_20` — «толстые хвосты» доходностей

- **Что показывает:** куртозис (kurtosis) доходностей r1 на окне 20 баров.
- **Интерпретация:** высокие значения → много экстремальных движений; низкие → ближе к нормальному распределению.
- **Комментарий:** индикатор аномальной активности/кластеризации.
- **Расчёт:** `kurtosis(r1_{t-19:t})`.

---

### 41) `f_autocorr_ret1_lag{k}` — автокорреляция доходности

- **Что показывает:** зависимость доходности r1 от её прошлых значений.
- **Интерпретация:** положительная → инерция (momentum); отрицательная → возврат к среднему (mean-reversion).
- **Комментарий:** можно брать лаги 1…5, тестировать разные окна (50–100 баров).
- **Расчёт:** `corr(r1_t, r1_{t-k})`.

---

## Давление к пробою (breakout pressure)

### 42) `f_donchian_dist_upper_20_over_atr14` — расстояние до верхней границы

- **Что показывает:** насколько близка цена закрытия к верхней границе Дончиана.
- **Интерпретация:** <1 → цена «подпирает» границу, возможен пробой.
- **Комментарий:** хорошо работает в связке с флагами сессий.
- **Расчёт:** `(High20_t − close_t)/ATR14_t`.

---

### 43) `f_donchian_dist_lower_20_over_atr14` — расстояние до нижней границы

- **Что показывает:** близость цены к нижней границе Дончиана.
- **Интерпретация:** <1 → давление вниз, возможен пробой.
- **Комментарий:** симметричен предыдущему признаку.
- **Расчёт:** `(close_t − Low20_t)/ATR14_t`.

---

### 44) `f_bars_since_upper_break_20` — баров после верхнего пробоя

- **Что показывает:** сколько баров прошло с момента выхода цены выше верхнего Дончиана.
- **Интерпретация:** малое значение → свежий пробой; большое → эффект угасает.
- **Комментарий:** кодирует «время с последнего события».
- **Расчёт:** счётчик баров с момента `close_t > High20_t`.

---

### 45) `f_bars_since_lower_break_20` — баров после нижнего пробоя

- **Что показывает:** сколько баров прошло с момента выхода ниже нижнего Дончиана.
- **Интерпретация:** аналогично верхнему пробою.
- **Комментарий:** даёт симметричный сигнал для шортов.
- **Расчёт:** счётчик баров с момента `close_t < Low20_t`.

---

## Сессии (sessions)

### 46) `f_sess_london_open` — флаг Лондонской сессии

- **Что показывает:** находится ли бар во временном окне Лондона (например, 8–16 GMT).
- **Интерпретация:** 1 → активная ликвидность Лондона; 0 → вне сессии.
- **Комментарий:** время суток сильно влияет на волатильность.
- **Расчёт:** `1[hour ∈ London_hours]`.

---

### 47) `f_sess_ny_open` — флаг Нью-Йоркской сессии

- **Что показывает:** принадлежность бара NY-сессии (13–22 GMT).
- **Интерпретация:** 1 → высокие объёмы, сильные движения.
- **Комментарий:** особенно важно для EURUSD.
- **Расчёт:** `1[hour ∈ NY_hours]`.

---

### 48) `f_sess_overlap` — флаг пересечения Лондон–Нью-Йорк

- **Что показывает:** бар приходится на часы, когда обе сессии активны.
- **Интерпретация:** часто максимальная волатильность и движения.
- **Комментарий:** даёт локальные пики импульсов.
- **Расчёт:** `1[hour ∈ overlap_hours]`.

---

## Целевые переменные (targets)

### A) `y_bhs` — трёхклассная `buy/hold/sell`

- **Суть:** метка направления через порог волатильности.
- **Расчёт:**
    
    `ret_H = (close_{t+H} - close_t) / close_t` *(или лог-доходность)*
    
    `ε_t = k·ATR_t` *(добавь спред/комиссию, если учитываешь)*
    
    Метка:
    
    - `buy`, если `ret_H > +ε_t`
    - `sell`, если `ret_H < -ε_t`
    - `hold`, иначе.
- **Без утечки (leakage):** все признаки берутся **на шаг H назад** относительно метки, т.е. фичи на времени `t` → цель на `t+H`.

### B) `y_bs` — бинарная `buy/sell`

- **Суть:** удалить «серую зону» `hold`, оставив только направленные классы.
- **Вариант 1 (жёсткий):**
    - Исключить наблюдения, где `|ret_H| ≤ ε_t`.
    - Метка: `buy`, если `ret_H > 0`; `sell`, если `ret_H < 0`.
- **Вариант 2 (пороговый):**
    - Метка: `buy`, если `ret_H > +ε_t`; `sell`, если `ret_H < -ε_t`;
    - Строки с `|ret_H| ≤ ε_t` — **дропнуть** из обучения.
- **Примечание:** вариант 2 даёт более «чистые» классы ценой снижения выборки.

---

## Короткий глоссарий (термины → определение и мини-пример)

- **Боковик (sideways market):** рынок без явного тренда; пример — цена колеблется в узком диапазоне, ADX<20.
- **Нормализация на ATR (ATR-normalization):** деление величины на ATR, чтобы сравнивать «сильность» движения при разных режимах волатильности; пример — отклонение `+1.5 ATR` считается заметным независимо от абсолютных пунктов.
- **Перекупленность/перепроданность (overbought/oversold):** состояние, где цена/осциллятор достигли статистически высоких/низких уровней; пример — `RSI>70`/`<30`, `z>+2`/`<-2`.
- **Кросс (cross):** пересечение двух средних; пример — EMA10 пересекает EMA20 снизу вверх → бычий сигнал.
- **Z-оценка (z-score):** стандартированное отклонение от среднего; пример — `z=+2` означает, что текущая цена выше среднего на 2 стандартных отклонения.
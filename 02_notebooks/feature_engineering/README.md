# 📊 Feature Engineering Notebooks

Эта папка содержит Jupyter notebooks для интерактивной разработки и тестирования feature engineering.

## 📋 Содержимое

### `feature_engineering_pipeline.ipynb`
**Основной notebook для создания и анализа признаков**

#### 🎯 Что делает:
 - Запуск FeatureEngineeringPipeline
 - Создание компактного MVP-набора из 33 признаков (единый нейминг `f_*` + `close`)
 - Визуализация ключевых признаков
 - Корреляционный анализ
 - Сохранение результатов в Parquet

#### 🚀 Быстрый старт:
1. Запустите все ячейки подряд для автоматического создания всех признаков
2. Результат: `df_with_features` - DataFrame с всеми признаками
3. Файлы сохраняются в `01_data/processed/`

#### 📊 Создаваемые признаки (MVP, 33 фичи):

- **Базовый уровень**
  - `close`

- **EMA**
  - `f_ema_10`, `f_ema_20`, `f_ema_50`, `f_ema_200`

- **Тренд**
  - `f_close_minus_ema_20_over_atr14`, `f_ema20_slope`, `f_macd_12_26_9_hist`, `f_di_diff_14`, `f_adx_14`

- **Импульс**
  - `f_rsi_5`, `f_rsi_14`, `f_roc_5`

- **Волатильность и диапазон**
  - `f_atr_14_pct`, `f_return_std_20`, `f_range_over_atr`, `f_donchian_pos_20`, `f_keltner_pos_20`, `f_percentB_20`, `f_bandwidth_20`

- **Доходности и статистика**
  - `f_ret_1`, `f_ret_6`, `f_ret_12`, `f_ret_24`, `f_ret_1_z20`, `f_rolling_mean_ret_5`, `f_rolling_std_ret_5`, `f_zscore_close_50`

- **Прочие**
  - `f_vwap_dev_over_atr14`, `f_psar_trend`

- **Календарные**
  - `f_hour_of_day_sin`, `f_hour_of_day_cos`, `f_dow`

Правила наименования: все признаки — `f_*`, слова через подчёркивание, числа — длины окон в барах (исключение: `close`).

#### 🔧 Кастомизация:
- Профили (`profile`) используются для настроек пайплайна (логирование, сохранение, обработка NaN). Состав признаков фиксирован (MVP, 33 фичи).
- Параметры сохранения/логирования настраиваются через `04_configs/feature_engineering.yml`.

#### 📈 Результат:
- Исходные данные: 5 колонок (OHLCV)
- Финальные данные: 33 колонки (`close` + `f_*`)
- Сохранение: Parquet с компрессией snappy
- Время выполнения: обычно значительно быстрее прежнего полного набора признаков

## 🎯 Следующие шаги:
1. Feature Selection
2. Создание целевых переменных
3. ML модели (LSTM, XGBoost)
4. Бэктестинг стратегий